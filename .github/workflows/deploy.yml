name: Deploy Code
on: [push, workflow_dispatch]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "echo test"
        run: |
          ssh -i ${{secrets.ID_RSA}} ubuntu@3.252.249.108 -o StrictHostKeyChecking=no
          touch ~/hello.txt
